/*
 * LoginContainer Messages
 *
 * This contains all the text for the LoginContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LoginContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoginContainer container!',
  },
});
