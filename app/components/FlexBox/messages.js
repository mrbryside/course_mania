/*
 * FlexBox Messages
 *
 * This contains all the text for the FlexBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FlexBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FlexBox component!',
  },
});
